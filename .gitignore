# Зависимости (самая тяжелая папка, на сервер попадает через npm install)
node_modules/

# Секретные данные (пароли к БД, ключи API)
.env
.env.local
.env.save

# Системные файлы macOS (те самые, что мешали нам в терминале)
.DS_Store

# Системные файлы Windows
Thumbs.db

# Логи ошибок (чтобы не путались под ногами)
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Папки сборки (если начнешь использовать React/Vue)
dist/
build/